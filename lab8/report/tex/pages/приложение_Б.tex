\section*{Приложение Б. Заголовочный файл и реализация класса \texttt{ContactDialog} и структуры в \texttt{contact.h}}
\addcontentsline{toc}{section}{Приложение Б. Заголовочный файл и реализация класса \texttt{ContactDialog} и структуры в \texttt{contact.h}}

\begin{lstlisting}[language=C++, numbers=left, basicstyle=\ttfamily\scriptsize]
// contact.h
#pragma once

#include <QString>
#include <QDate>
#include <QList>
#include <QDataStream>

struct PhoneNumber {
    enum Type { Work, Home, Mobile };

    Type type;
    QString number;

    PhoneNumber(Type t = Mobile, const QString& n = "")
        : type(t), number(n) {}

    QString typeString() const {
        switch (type) {
            case Work: return "Рабочий";
            case Home: return "Домашний";
            case Mobile: return "Мобильный";
            default: return "Другой";
        }
    }

    QString formatted() const {
        if (number.length() == 11 && number.startsWith("7")) {
            return QString("+%1 (%2) %3-%4-%5")
                .arg(number.mid(0, 1))
                .arg(number.mid(1, 3))
                .arg(number.mid(4, 3))
                .arg(number.mid(7, 2))
                .arg(number.mid(9, 2));
        }
        return "+" + number;
    }
};

struct Contact {
    QString firstName;
    QString lastName;
    QString middleName;
    QString address;
    QDate birthDate;
    QString email;
    QList<PhoneNumber> phones;

    QString fullName() const {
        return QString("%1 %2 %3").arg(lastName, firstName, middleName).trimmed();
    }

    QString primaryPhone() const {
        return phones.isEmpty() ? "" : phones.first().formatted();
    }

    QString allPhones() const {
        QStringList list;
        for (const auto& phone : phones) {
            list << QString("%1: %2").arg(phone.typeString(), phone.formatted());
        }
        return list.join("; ");
    }
};

inline QDataStream& operator<<(QDataStream& out, const PhoneNumber& phone) {
    out << static_cast<int>(phone.type) << phone.number;
    return out;
}

inline QDataStream& operator>>(QDataStream& in, PhoneNumber& phone) {
    int type;
    in >> type >> phone.number;
    phone.type = static_cast<PhoneNumber::Type>(type);
    return in;
}

inline QDataStream& operator<<(QDataStream& out, const Contact& contact) {
    out << contact.firstName
        << contact.lastName
        << contact.middleName
        << contact.address
        << contact.birthDate
        << contact.email
        << contact.phones;
    return out;
}

inline QDataStream& operator>>(QDataStream& in, Contact& contact) {
    in >> contact.firstName
       >> contact.lastName
       >> contact.middleName
       >> contact.address
       >> contact.birthDate
       >> contact.email
       >> contact.phones;
    return in;
}


// createcontact.h
#pragma once

#include <QDialog>
#include <QLineEdit>
#include <QDateEdit>
#include <QTableWidget>
#include <QComboBox>
#include <QPushButton>
#include <QLabel>
#include "contact.h"

class ContactDialog : public QDialog {
    Q_OBJECT
public:
    explicit ContactDialog(QWidget* parent = nullptr);

    void setContact(const Contact& contact);
    Contact getContact() const;

private slots:
    void addPhone();
    void removePhone();
    void validate();

private:
    void setupUI();
    void updatePhoneTable();
    bool validateAll();
    void showError(QLabel* label, const QString& error);
    void clearError(QLabel* label);

    QLineEdit* m_lastNameEdit;
    QLineEdit* m_firstNameEdit;
    QLineEdit* m_middleNameEdit;
    QLineEdit* m_addressEdit;
    QDateEdit* m_birthDateEdit;
    QLineEdit* m_emailEdit;

    QTableWidget* m_phonesTable;
    QLineEdit* m_phoneInput;
    QComboBox* m_phoneTypeCombo;

    QLabel* m_lastNameError;
    QLabel* m_firstNameError;
    QLabel* m_middleNameError;
    QLabel* m_addressError;
    QLabel* m_birthDateError;
    QLabel* m_emailError;
    QLabel* m_phoneError;

    QPushButton* m_okButton;
    QPushButton* m_cancelButton;

    QList<PhoneNumber> m_phones;
};

// createcontact.cpp
#include <QVBoxLayout>
#include <QHBoxLayout>
#include <QFormLayout>
#include <QGroupBox>
#include <QHeaderView>
#include <QMessageBox>

#include "createcontact.h"
#include "validator.h"

ContactDialog::ContactDialog(QWidget* parent) : QDialog(parent) {
    setupUI();
    setWindowTitle("Создать контакт");
    resize(500, 600);
}

void ContactDialog::setupUI() {
    QVBoxLayout* mainLayout = new QVBoxLayout(this);

    QGroupBox* nameGroup = new QGroupBox("ФИО", this);
    QFormLayout* nameLayout = new QFormLayout(nameGroup);

    m_lastNameEdit = new QLineEdit(this);
    m_lastNameError = new QLabel(this);
    m_lastNameError->setStyleSheet("color: red; font-size: 10px;");

    m_firstNameEdit = new QLineEdit(this);
    m_firstNameError = new QLabel(this);
    m_firstNameError->setStyleSheet("color: red; font-size: 10px;");

    m_middleNameEdit = new QLineEdit(this);
    m_middleNameError = new QLabel(this);
    m_middleNameError->setStyleSheet("color: red; font-size: 10px;");

    QVBoxLayout* lastNameLayout = new QVBoxLayout();
    lastNameLayout->addWidget(m_lastNameEdit);
    lastNameLayout->addWidget(m_lastNameError);
    nameLayout->addRow("Фамилия*:", lastNameLayout);

    QVBoxLayout* firstNameLayout = new QVBoxLayout();
    firstNameLayout->addWidget(m_firstNameEdit);
    firstNameLayout->addWidget(m_firstNameError);
    nameLayout->addRow("Имя*:", firstNameLayout);

    QVBoxLayout* middleNameLayout = new QVBoxLayout();
    middleNameLayout->addWidget(m_middleNameEdit);
    middleNameLayout->addWidget(m_middleNameError);
    nameLayout->addRow("Отчество:", middleNameLayout);

    mainLayout->addWidget(nameGroup);

    QGroupBox* contactGroup = new QGroupBox("Контактные данные", this);
    QFormLayout* contactLayout = new QFormLayout(contactGroup);

    m_addressEdit = new QLineEdit(this);
    m_addressError = new QLabel(this);
    m_addressError->setStyleSheet("color: red; font-size: 10px;");

    m_birthDateEdit = new QDateEdit(this);
    m_birthDateEdit->setCalendarPopup(true);
    m_birthDateEdit->setDate(QDate(2000, 1, 1));
    m_birthDateEdit->setMaximumDate(QDate::currentDate().addDays(-1));
    m_birthDateError = new QLabel(this);
    m_birthDateError->setStyleSheet("color: red; font-size: 10px;");

    m_emailEdit = new QLineEdit(this);
    m_emailError = new QLabel(this);
    m_emailError->setStyleSheet("color: red; font-size: 10px;");

    QVBoxLayout* addressLayout = new QVBoxLayout();
    addressLayout->addWidget(m_addressEdit);
    addressLayout->addWidget(m_addressError);
    contactLayout->addRow("Адрес*:", addressLayout);

    QVBoxLayout* birthLayout = new QVBoxLayout();
    birthLayout->addWidget(m_birthDateEdit);
    birthLayout->addWidget(m_birthDateError);
    contactLayout->addRow("Дата рождения*:", birthLayout);

    QVBoxLayout* emailLayout = new QVBoxLayout();
    emailLayout->addWidget(m_emailEdit);
    emailLayout->addWidget(m_emailError);
    contactLayout->addRow("Email*:", emailLayout);

    mainLayout->addWidget(contactGroup);

    QGroupBox* phoneGroup = new QGroupBox("Телефоны", this);
    QVBoxLayout* phoneLayout = new QVBoxLayout(phoneGroup);

    QHBoxLayout* phoneInputLayout = new QHBoxLayout();
    m_phoneTypeCombo = new QComboBox(this);
    m_phoneTypeCombo->addItem("Мобильный", PhoneNumber::Mobile);
    m_phoneTypeCombo->addItem("Рабочий", PhoneNumber::Work);
    m_phoneTypeCombo->addItem("Домашний", PhoneNumber::Home);

    m_phoneInput = new QLineEdit(this);
    m_phoneInput->setPlaceholderText("+7 (999) 123-45-67");

    QPushButton* addPhoneBtn = new QPushButton("Добавить", this);
    QPushButton* removePhoneBtn = new QPushButton("Удалить", this);

    phoneInputLayout->addWidget(m_phoneTypeCombo);
    phoneInputLayout->addWidget(m_phoneInput, 1);
    phoneInputLayout->addWidget(addPhoneBtn);
    phoneInputLayout->addWidget(removePhoneBtn);

    phoneLayout->addLayout(phoneInputLayout);

    m_phoneError = new QLabel(this);
    m_phoneError->setStyleSheet("color: red; font-size: 10px;");
    phoneLayout->addWidget(m_phoneError);

    // Таблица телефонов
    m_phonesTable = new QTableWidget(this);
    m_phonesTable->setColumnCount(2);
    m_phonesTable->setHorizontalHeaderLabels({"Тип", "Номер"});
    m_phonesTable->horizontalHeader()->setStretchLastSection(true);
    m_phonesTable->setSelectionBehavior(QAbstractItemView::SelectRows);
    m_phonesTable->setEditTriggers(QAbstractItemView::NoEditTriggers);
    phoneLayout->addWidget(m_phonesTable);

    mainLayout->addWidget(phoneGroup);

    // Кнопки OK/Cancel
    QHBoxLayout* buttonLayout = new QHBoxLayout();
    buttonLayout->addStretch();

    m_okButton = new QPushButton("Добавить", this);
    m_cancelButton = new QPushButton("Отмена", this);

    buttonLayout->addWidget(m_okButton);
    buttonLayout->addWidget(m_cancelButton);

    mainLayout->addLayout(buttonLayout);

    // Соединения
    connect(addPhoneBtn, &QPushButton::clicked, this, &ContactDialog::addPhone);
    connect(removePhoneBtn, &QPushButton::clicked, this, &ContactDialog::removePhone);
    connect(m_okButton, &QPushButton::clicked, this, &ContactDialog::validate);
    connect(m_cancelButton, &QPushButton::clicked, this, &QDialog::reject);
}

void ContactDialog::setContact(const Contact& contact) {
    m_lastNameEdit->setText(contact.lastName);
    m_firstNameEdit->setText(contact.firstName);
    m_middleNameEdit->setText(contact.middleName);
    m_addressEdit->setText(contact.address);
    m_birthDateEdit->setDate(contact.birthDate);
    m_emailEdit->setText(contact.email);
    m_phones = contact.phones;
    updatePhoneTable();
}

Contact ContactDialog::getContact() const {
    Contact contact;

    auto lastNameResult = Validator::validateName(m_lastNameEdit->text());
    auto firstNameResult = Validator::validateName(m_firstNameEdit->text());
    auto middleNameResult = Validator::validateName(m_middleNameEdit->text());
    auto addressResult = Validator::validateAddress(m_addressEdit->text());
    auto emailResult = Validator::validateEmail(m_emailEdit->text());

    contact.lastName = lastNameResult.cleaned;
    contact.firstName = firstNameResult.cleaned;
    contact.middleName = middleNameResult.cleaned;
    contact.address = addressResult.cleaned;
    contact.birthDate = m_birthDateEdit->date();
    contact.email = emailResult.cleaned;
    contact.phones = m_phones;

    return contact;
}

void ContactDialog::addPhone() {
    auto result = Validator::validatePhone(m_phoneInput->text());

    if (!result.valid) {
        showError(m_phoneError, result.error);
        return;
    }

    clearError(m_phoneError);

    PhoneNumber phone;
    phone.type = static_cast<PhoneNumber::Type>(m_phoneTypeCombo->currentData().toInt());
    phone.number = result.cleaned;

    m_phones.append(phone);
    updatePhoneTable();
    m_phoneInput->clear();
}

void ContactDialog::removePhone() {
    int row = m_phonesTable->currentRow();
    if (row >= 0 && row < m_phones.size()) {
        m_phones.removeAt(row);
        updatePhoneTable();
    }
}

void ContactDialog::updatePhoneTable() {
    m_phonesTable->setRowCount(m_phones.size());

    for (int i = 0; i < m_phones.size(); ++i) {
        m_phonesTable->setItem(i, 0, new QTableWidgetItem(m_phones[i].typeString()));
        m_phonesTable->setItem(i, 1, new QTableWidgetItem(m_phones[i].formatted()));
    }
}

void ContactDialog::showError(QLabel* label, const QString& error) {
    label->setText(error);
}

void ContactDialog::clearError(QLabel* label) {
    label->clear();
}

bool ContactDialog::validateAll() {
    bool valid = true;

    // Фамилия
    auto lastNameResult = Validator::validateName(m_lastNameEdit->text());
    if (!lastNameResult.valid) {
        showError(m_lastNameError, lastNameResult.error);
        valid = false;
    } else {
        clearError(m_lastNameError);
    }

    // Имя
    auto firstNameResult = Validator::validateName(m_firstNameEdit->text());
    if (!firstNameResult.valid) {
        showError(m_firstNameError, firstNameResult.error);
        valid = false;
    } else {
        clearError(m_firstNameError);
    }

    // Отчество (необязательное)
    if (!m_middleNameEdit->text().trimmed().isEmpty()) {
        auto middleNameResult = Validator::validateName(m_middleNameEdit->text());
        if (!middleNameResult.valid) {
            showError(m_middleNameError, middleNameResult.error);
            valid = false;
        } else {
            clearError(m_middleNameError);
        }
    } else {
        clearError(m_middleNameError);
    }

    // Адрес
    auto addressResult = Validator::validateAddress(m_addressEdit->text());
    if (!addressResult.valid) {
        showError(m_addressError, addressResult.error);
        valid = false;
    } else {
        clearError(m_addressError);
    }

    // Дата рождения
    auto birthResult = Validator::validateBirthDate(m_birthDateEdit->date());
    if (!birthResult.valid) {
        showError(m_birthDateError, birthResult.error);
        valid = false;
    } else {
        clearError(m_birthDateError);
    }

    // Email
    auto emailResult = Validator::validateEmail(m_emailEdit->text());
    if (!emailResult.valid) {
        showError(m_emailError, emailResult.error);
        valid = false;
    } else {
        clearError(m_emailError);
    }

    // Минимум один телефон
    if (m_phones.isEmpty()) {
        showError(m_phoneError, "Добавьте хотя бы один телефон");
        valid = false;
    } else {
        clearError(m_phoneError);
    }

    return valid;
}

void ContactDialog::validate() {
    if (validateAll()) {
        accept();
    }
}

\end{lstlisting}